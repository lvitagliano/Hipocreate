import React, { lazy } from 'react'
import { Fragmento } from './style'
import { LoginComponent } from '../components/Login';

export const Login = () => {

    return (
        <Fragmento>
             <LoginComponent />
        </Fragmento>
    )
}
